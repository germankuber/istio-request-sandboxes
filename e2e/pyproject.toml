[project]
name = "e2e"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx==0.28.1",
]

[dependency-groups]
dev = [
    "pytest==8.3.4",
]

[tool.pytest.ini_options]
markers = [
    "disruptive: kills pods or scales deployments; restores cluster state on its own",
]
