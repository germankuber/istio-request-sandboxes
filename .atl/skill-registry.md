# Skill Registry — TestMultiServices

<!-- Auto-generated by gentle-ai skill-registry refresh. Run `gentle-ai skill-registry refresh --force` to regenerate. -->

Last updated: 2026-09-22

## Sources scanned

- /Users/GermanKuber/.agents/skills
- /Users/GermanKuber/.claude/skills
- /Users/GermanKuber/.codex/skills
- /Users/GermanKuber/.codeium/windsurf/skills
- /Users/GermanKuber/.openclaw/skills

## Contract

**Delegator use only.** This registry is an index, not a summary. Any agent that launches subagents reads it to select relevant skills, then passes exact `SKILL.md` paths for the subagent to read before work.

`SKILL.md` remains the source of truth. Do not inject generated summaries or compact rules by default; pass paths so subagents load the full runtime contract and preserve author intent.

## Skills

| Skill | Trigger / description | Scope | Path |
| --- | --- | --- | --- |
| `architecture-analyzer` | Trigger: documentar capacidades de endpoints, capability docs, indexar endpoints, qué hace cada endpoint, documentar para búsqueda semántica. Documents every HTTP endpoint's business capability into the code's own OpenAPI annotations, so an agent can semantically retrieve endpoints across repositories and decide where a new requirement belongs. | user | `/Users/GermanKuber/.claude/skills/architecture-analyzer/SKILL.md` |
| `branch-pr` | Create Gentle AI pull requests with issue-first checks. Trigger: creating, opening, or preparing PRs for review. | user | `/Users/GermanKuber/.claude/skills/branch-pr/SKILL.md` |
| `chained-pr` | Trigger: PRs over 400 lines, stacked PRs, review slices. Split oversized changes into chained PRs that protect review focus. | user | `/Users/GermanKuber/.claude/skills/chained-pr/SKILL.md` |
| `codebase-memory` | Use the codebase knowledge graph for structural code queries. Triggers on: explore the codebase, understand the architecture, what functions exist, show me the structure, who calls this function, what does X call, trace the call chain, find callers of, show dependencies, impact analysis, dead code, unused functions, high fan-out, refactor candidates, code quality audit, graph query syntax, Cypher query examples, edge types, how to use search_graph. | user | `/Users/GermanKuber/.claude/skills/codebase-memory/SKILL.md` |
| `cognitive-doc-design` | Design docs that reduce cognitive load. Trigger: writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs. | user | `/Users/GermanKuber/.claude/skills/cognitive-doc-design/SKILL.md` |
| `comment-writer` | Write warm, direct collaboration comments. Trigger: PR feedback, issue replies, reviews, Slack messages, or GitHub comments. | user | `/Users/GermanKuber/.claude/skills/comment-writer/SKILL.md` |
| `dotnet-conventions` | Trigger: writing or reviewing C#/.NET code, .cs files, csproj, naming, formatting. Apply official Microsoft/dotnet-runtime C# conventions. | user | `/Users/GermanKuber/.claude/skills/dotnet-conventions/SKILL.md` |
| `go-testing` | Trigger: Go tests, go test coverage, Bubbletea teatest, golden files. Apply focused Go testing patterns. | user | `/Users/GermanKuber/.claude/skills/go-testing/SKILL.md` |
| `graphify` | Use for any question about a codebase, its architecture, file relationships, or project content — especially when graphify-out/ exists, where the question should be treated as a graphify query first. Turns any input (code, docs, papers, images, videos) into a persistent knowledge graph with god nodes, community detection, and query/path/explain tools. | user | `/Users/GermanKuber/.claude/skills/graphify/SKILL.md` |
| `issue-creation` | Create Gentle AI issues with issue-first checks. Trigger: creating GitHub issues, bug reports, or feature requests. | user | `/Users/GermanKuber/.claude/skills/issue-creation/SKILL.md` |
| `judgment-day` | Trigger: judgment day, dual review, adversarial review, juzgar. Run blind dual review, fix confirmed issues, then re-judge. | user | `/Users/GermanKuber/.claude/skills/judgment-day/SKILL.md` |
| `lavish` | Turn complex or visual agent responses into rich, reviewable HTML artifacts the user can annotate and send feedback on, using the lavish-axi CLI. Use when about to give a plan, comparison, diagram, table, code diff, report, or anything easier to grasp visually than as prose. | user | `/Users/GermanKuber/.agents/skills/lavish/SKILL.md` |
| `multi-repo-design` | Trigger: implement requirement across microservices, multi-repo feature, which services are involved, design before coding, cross-service change. Graph-first requirement analysis producing an approval-gated TO-BE design. | user | `/Users/GermanKuber/.claude/skills/multi-repo-design/SKILL.md` |
| `nera-branddeck` | Genera presentaciones (.pptx pensadas para Google Slides) 100% on-brand para Nera — institucional, productores o proveedores. Usar siempre que se pida un deck, presentación, pitch o slides para Nera. | user | `/Users/GermanKuber/.claude/skills/nera-branddeck/SKILL.md` |
| `obsidian-book-summarizer` | Summarizes book chapters and writes them as Obsidian notes under a Libros/<Book Name>/ folder, with one note per chapter (dense summary + key ideas + verbatim quotes + actionables, written in Spanish) and a book MOC that accumulates the thesis and cross-cutting ideas. Use this skill when the user pastes (or passes) the text of a book chapter and wants to summarize it / add it to their vault, mentions "resumir un libro", "mis libros", "este capítulo", or asks for a reading note. The skill ALWAYS asks the user for the book name, chapter number, and title ONE FIELD AT A TIME in separate turns (a pick-list when there are existing books to choose from, a short free-text prompt otherwise) — matching the book against ones that already exist (tolerant of typos), and never inferring book/number from the text. NOT for standalone web articles (that's obsidian-knowledge-extractor) nor for an ephemeral chat summary — this produces durable, wikilinked reading notes with frontmatter, organized by book. | user | `/Users/GermanKuber/.claude/skills/obsidian-book-summarizer/SKILL.md` |
| `obsidian-knowledge-extractor` | Extract knowledge from a web article, blog post, or pasted text and turn it into well-structured Obsidian markdown notes that integrate with the user's existing vault as a Zettelkasten-style knowledge graph. Use this whenever the user provides a URL or pasted article content AND mentions Obsidian, their vault, "my notes", "my knowledge base", or asks to "extract", "process", "add to my vault", or "turn this into notes". Make sure to use this skill even when the user never says the word "Obsidian" — as long as the goal is durable, atomic, wikilinked personal-knowledge notes with frontmatter built from source material, this is the skill, not a plain chat summary. Use THIS skill (which runs a review-and-approve plan step before writing) by default for that goal; the separate auto-extract skill is the no-gate one-shot variant for when the user explicitly wants it written immediately without a plan. Do NOT trigger for a quick in-chat summary, casual reading help, or generic web research with no vault destination. | user | `/Users/GermanKuber/.claude/skills/obsidian-knowledge-extractor/SKILL.md` |
| `prs` | Manage open pull requests on the current cwd's git repository. Provides three subcommands: `status` (dashboard of all your open PRs), `sync <PR#>` (rebase a PR branch on top of its base, handling conflicts turn-by-turn) and `review <PR#>` (iterate over unresolved review threads with options to reply, resolve, or apply suggested changes).  When invoked with no PR number, commands operate on the PR of the currently checked-out branch. Use this skill whenever the user mentions "their PRs", "comments to address", "rebase against master", "outdated branch", or asks about the state of any pull request they own. | user | `/Users/GermanKuber/.claude/skills/prs/SKILL.md` |
| `python-backend` | Best practices for building, reviewing, or refactoring production Python backends (Python 3.11/3.12/3.13) with FastAPI, Pydantic 2, SQLAlchemy 2 (async), and pytest, in a hexagonal / layered architecture with strict TDD. Use this skill whenever writing or reviewing backend Python: FastAPI routers, dependencies, middleware or SSE endpoints; Pydantic models, DTOs, settings, or validators; SQLAlchemy ORM models, repositories, queries, or Alembic migrations; async/await code; pytest tests, fixtures, or test doubles; and domain/application/infrastructure/api layering, Protocol ports, dependency inversion, frozen dataclasses, and the "classes not dicts" rule. Trigger on .py files and on FastAPI/Pydantic/SQLAlchemy/pytest/async work even when the user does not say "best practices" — any backend Python task should consult it. | user | `/Users/GermanKuber/.claude/skills/python-backend/SKILL.md` |
| `reading-mark` | Mark Obsidian vault notes as fully read (100%) — reset their `reading:` frontmatter so `read_words = total_words`, `pct = 100`, and `last_read = today` — for a single note OR a whole category/folder. Use this whenever the user says they've read something and wants the tracker to reflect it: "marcá este file como leído", "ya leí toda la categoría AI", "mark this note read", "marcá leído rag", "ponelo en 100%", "leí estas notas". It accepts a note name/path or a folder/category; for a category it marks every note inside (recursively). It also initializes the `reading:` block on untracked notes the first time it touches them (counting the body words and assuming you've read them). This WRITES to the vault. To only VIEW what's pending without changing anything, use the sibling read-only skill `reading-status`. NOT for summarizing articles or creating notes — that is `obsidian-knowledge-extractor`. | user | `/Users/GermanKuber/.claude/skills/reading-mark/SKILL.md` |
| `reading-status` | Report which Obsidian vault notes have unread (newly-added, not-yet-consumed) content, based on the per-note `reading:` frontmatter (total_words / read_words / pct) maintained alongside the knowledge-extractor pipeline. Use this whenever the user asks to see their reading status / pending content — "qué me falta leer", "mostrame los porcentajes", "qué se agregó y no leí", "reading status", "what's pending", "qué notas tienen contenido nuevo". It shows ONLY notes below 100% read (notes fully consumed are omitted), grouped by category/folder, with the pending word count per note and per category. This skill is READ-ONLY: it never modifies notes. To mark something as read (reset a note or a whole category to 100%), use the sibling skill `reading-mark` instead. NOT for summarizing an article or writing notes — that is `obsidian-knowledge-extractor`. | user | `/Users/GermanKuber/.claude/skills/reading-status/SKILL.md` |
| `seven-advisors` | Seven Advisors decision council - structured multi-perspective deliberation for important decisions. Use when facing complex choices, strategic decisions, or when you need to think through a problem from multiple angles. Triggers on "seven advisors", "thinking hats", "decision council", "advisors council", "deliberation", "think through this decision", or "multiple perspectives". | user | `/Users/GermanKuber/.claude/skills/seven-advisors/SKILL.md` |
| `skill-creator` | Trigger: new skills, agent instructions, documenting AI usage patterns. Create LLM-first skills with valid frontmatter. | user | `/Users/GermanKuber/.claude/skills/skill-creator/SKILL.md` |
| `skill-improver` | Trigger: improve skills, audit skills, refactor skills, skill quality. Audit and upgrade existing LLM-first skills. | user | `/Users/GermanKuber/.claude/skills/skill-improver/SKILL.md` |
| `vercel-react-best-practices` | React and Next.js performance optimization guidelines from Vercel Engineering. This skill should be used when writing, reviewing, or refactoring React/Next.js code to ensure optimal performance patterns. Triggers on tasks involving React components, Next.js pages, data fetching, bundle optimization, or performance improvements. | user | `/Users/GermanKuber/.claude/skills/react-best-practices/SKILL.md` |
| `wiki-lint` | Check the structural health of an Obsidian vault and report problems — broken/dead links, intentional seeds, orphan notes, stubs, tag-model violations, broken embeds, and folders missing a MOC. Use this whenever the user asks to audit or sanity-check their vault — "revisá mi vault", "wiki lint", "qué links están rotos", "hay notas huérfanas", "chequeá el vault", "lint my vault", "find dead links", "vault health", "qué notas están sin terminar", "faltan MOCs". It reports grouped by severity and category, and is strictly READ-ONLY: it surfaces problems, it NEVER writes to or fixes the vault. To create or edit notes use `obsidian-knowledge-extractor`; to change reading state use `reading-mark`. | user | `/Users/GermanKuber/.claude/skills/wiki-lint/SKILL.md` |
| `wiki-query` | Answer questions from the user's own Obsidian vault — GROUNDED and READ-ONLY. Use whenever the user asks their notes/vault/wiki something: "preguntá a mi wiki", "qué sé sobre X", "según mis notas", "qué dice mi vault de …", "buscá en mi wiki", "consultá mis notas sobre …", "what do my notes say about…". It answers ONLY from what's written in the vault — it NEVER fabricates from the model's own (parametric) knowledge, and when the vault is silent it says so plainly ("no encontré nada sobre X en tus notas") instead of inventing. It reads notes but never writes them. To ADD knowledge to the vault use `obsidian-knowledge-extractor`; to check reading state use `reading-status`. | user | `/Users/GermanKuber/.claude/skills/wiki-query/SKILL.md` |
| `work-unit-commits` | Plan commits as reviewable work units. Trigger: implementation, commit splitting, chained PRs, or keeping tests and docs with code. | user | `/Users/GermanKuber/.claude/skills/work-unit-commits/SKILL.md` |

## Loading protocol

1. Match task context and target files against the `Trigger / description` column.
2. Pass only the matching `Path` values to the subagent under `## Skills to load before work`.
3. Instruct the subagent to read those exact `SKILL.md` files before reading, writing, reviewing, testing, or creating artifacts.
4. If no matching skill exists, proceed without project skill injection and report `skill_resolution: none`.
