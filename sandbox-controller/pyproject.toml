[project]
name = "sandbox-controller"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "kopf==1.37.4",
    "kubernetes==31.0.0",
    "httpx==0.28.1",
]

[dependency-groups]
dev = [
    "pytest==8.3.4",
]
